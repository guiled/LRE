//import esbuild from "esbuild";
//import { getEsBuildConfig } from "../../builder/config";

describe("Builder", () => {
  test("It builds correctly", async () => {
    // await esbuild.build(getEsBuildConfig("src/index.ts", "/dev/null", false));
    expect(true).toBe(true);
  });
});
